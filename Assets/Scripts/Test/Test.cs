using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("测试成功，成功忽略掉了已经提交的文件");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
